---
title: "작성 중: 프로젝트명"
published: false
date:
---

<!--
  자유롭게 작성하는 프로젝트 초고 템플릿이다.

  - 절 순서, 번호, Markdown, Category, Tag, 제목, 파일명을 정하지 않고 아무 형식으로 적어도 된다.
  - 작성완료 시 사용자가 적은 내용만으로 노션 스타일의 구조와 강조를 적용한다.
  - 원문에 없는 기능, 수치, 실행 결과, 코드, 의사결정, 문제 해결 경험은 추가하지 않는다.
  - 불확실한 표현은 확정된 사실로 바꾸지 않는다.
  - 설명이 부족하거나 서로 모순되는 부분은 임의로 채우지 않고
    "추가로 작성하면 좋은 부분"으로 별도 보고한다.
  - 코드 / 실행 결과 / 에러 로그는 복사해서 그대로 붙여넣으면 된다.
    code fence(```)로 감싸지 않아도 자동으로 코드 블록이 된다.
  - 이미지는 `workspace/images/projects/`에 `project01_1.png` 형식(초고 파일명 + 넘버링)으로 올려둔다.
  - 실제로 만든 결과물이 있을 때만 Projects로 발행된다. 만들다 만 상태이거나
    개념 학습이 중심이면 Notes로 정리된다.
  - 겪은 오류를 깊이 파고든 기록은 Troubleshooting으로 분리하는 편이 낫다.
    따로 빼고 싶으면 그렇게 적어두면 된다.
  - 필요 없는 절은 지우고 필요한 절은 자유롭게 추가한다.
  - `date`에 YYYY-MM-DD를 적어두면 그 날짜를 발행일로 쓴다. 비워두면 작성완료를
    요청한 당일로 확정한다.
  - 완료하면 "작성완료 workspace/projects/project-번호.md"라고 요청한다.
  - workspace 전체는 사이트 빌드에서 제외되지만 공개 GitHub 저장소에서는 볼 수 있다.
-->

## 무엇을 만들었나

한 줄로 설명한다.

## 왜 만들었나

어떤 문제를 풀고 싶었는지, 무엇이 불편해서 시작했는지 적는다.

## 목표

이번 프로젝트에서 어디까지 하려고 했는지 적는다.

## 기술 스택

실제로 사용한 언어 / 라이브러리 / 도구만 적는다.

## 구조 / 동작 흐름

파일 구성, 데이터가 흘러가는 순서, 주요 부분의 역할을 적는다.

## 만든 기능

## 고민한 선택

무엇과 무엇을 두고 고민했고, 왜 그 선택을 했는지 적는다.

## 막혔던 문제와 해결

실제로 겪은 것만 적는다. 에러 메시지는 그대로 붙여넣는다.

## 코드

핵심 부분만 붙여넣는다. 전체를 넣지 않아도 된다.

## 실행 결과

수치, 출력, 화면 등 실제로 나온 것을 적는다.

## 배운 점

## 관련 링크

- GitHub Repository:
